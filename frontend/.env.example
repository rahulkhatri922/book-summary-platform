# Base URL of the Django API (note the /api prefix).
# Local dev against `python manage.py runserver` (port 8000):
VITE_API_URL=http://localhost:8000/api
# For the nginx production build, .env.production sets this to "/api" (same-origin,
# proxied to the backend container).
